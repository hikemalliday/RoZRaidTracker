services:
  app:
    build: .
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./roz_loot_tracker:/roz_loot_tracker
    ports:
      - "8000:8000"
    environment:
      - DJANGO_DB=${DJANGO_DB:-dev-db}